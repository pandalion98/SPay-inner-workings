package android.dirEncryption;

import android.os.Binder;
import android.os.IBinder;
import android.os.IInterface;
import android.os.Parcel;
import android.os.RemoteException;

public interface IDirEncryptionManagerCallBack extends IInterface {

    public static abstract class Stub extends Binder implements IDirEncryptionManagerCallBack {
        private static final String DESCRIPTOR = "android.dirEncryption.IDirEncryptionManagerCallBack";
        static final int TRANSACTION_updateStatus = 1;

        private static class Proxy implements IDirEncryptionManagerCallBack {
            private IBinder mRemote;

            Proxy(IBinder remote) {
                this.mRemote = remote;
            }

            public IBinder asBinder() {
                return this.mRemote;
            }

            public String getInterfaceDescriptor() {
                return Stub.DESCRIPTOR;
            }

            public void updateStatus(boolean status, int errorCode) throws RemoteException {
                int i = 1;
                Parcel _data = Parcel.obtain();
                Parcel _reply = Parcel.obtain();
                try {
                    _data.writeInterfaceToken(Stub.DESCRIPTOR);
                    if (!status) {
                        i = 0;
                    }
                    _data.writeInt(i);
                    _data.writeInt(errorCode);
                    this.mRemote.transact(1, _data, _reply, 0);
                    _reply.readException();
                } finally {
                    _reply.recycle();
                    _data.recycle();
                }
            }
        }

        public Stub() {
            attachInterface(this, DESCRIPTOR);
        }

        public static IDirEncryptionManagerCallBack asInterface(IBinder obj) {
            if (obj == null) {
                return null;
            }
            IInterface iin = obj.queryLocalInterface(DESCRIPTOR);
            if (iin == null || !(iin instanceof IDirEncryptionManagerCallBack)) {
                return new Proxy(obj);
            }
            return (IDirEncryptionManagerCallBack) iin;
        }

        public IBinder asBinder() {
            return this;
        }

        public boolean onTransact(int code, Parcel data, Parcel reply, int flags) throws RemoteException {
            switch (code) {
                case 1:
                    data.enforceInterface(DESCRIPTOR);
                    updateStatus(data.readInt() != 0, data.readInt());
                    reply.writeNoException();
                    return true;
                case IBinder.INTERFACE_TRANSACTION /*1598968902*/:
                    reply.writeString(DESCRIPTOR);
                    return true;
                default:
                    return super.onTransact(code, data, reply, flags);
            }
        }
    }

    void updateStatus(boolean z, int i) throws RemoteException;
}
